name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build: 
    runs_on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - name: Run make hello
      run: make hello
    
